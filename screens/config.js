/**
 * screens/config.js — shared network config for all Musician screens.
 * Change SYNC_URL here once to update all team-connected screens.
 */

// Sync server: runs on your Mac, same Wi-Fi network as all devices
export const SYNC_URL = 'http://10.0.0.34:8099';

// CineStage AI REST API (start with ~/Desktop/start_cinestage.sh)
export const CINESTAGE_URL = 'http://10.0.0.34:8000';
