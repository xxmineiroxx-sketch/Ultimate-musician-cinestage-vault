/**
 * Ultimate Playback — Sync Server Configuration
 *
 * LOCAL (development / simulator):  http://localhost:8099
 * PRODUCTION:                        https://sync.ultimatelabs.co
 *
 * Change SYNC_URL here to switch environments for all screens at once.
 * Ultimate Musician publishes to the same server via http://10.0.0.34:8099
 * (same machine — both resolve to the local UltimateSyncServer process).
 */
export const SYNC_URL = 'http://localhost:8099';
