# Setlist & Delivery (Organizer → Playback)

## Versioning
- Each publish increments service version
- Playback sync refreshes if version changed

## Service Pack payload
- service metadata
- ordered setlist
- per song: planned key/bpm, notes, role-specific chart links, media (permissions)
- offline download support
